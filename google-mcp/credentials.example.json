{
  "_comment": "This is a TEMPLATE showing the shape of the file. Do NOT edit this. Instead, download YOUR real OAuth client JSON from Google Cloud Console (see docs/GOOGLE_OAUTH.md), rename it to credentials.json, and drop it in this folder. credentials.json is gitignored and will never be committed.",
  "installed": {
    "client_id": "YOUR_CLIENT_ID.apps.googleusercontent.com",
    "project_id": "your-project-id",
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_uri": "https://oauth2.googleapis.com/token",
    "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "client_secret": "YOUR_CLIENT_SECRET",
    "redirect_uris": ["http://localhost:3000"]
  }
}
